meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{laravel_base_url}}/api/health
}

headers {
  Accept: application/json
}

docs {
  summary: Check API health
  description: |
    Public endpoint to check if the Top Frag API is running.
    Returns service status and timestamp.
} 